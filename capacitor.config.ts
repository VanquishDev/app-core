import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.vanquish.app-core',
  appName: 'app',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
